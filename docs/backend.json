{
  "entities": {
    "UserStreak": {
      "title": "UserStreak",
      "type": "object",
      "description": "Represents a user's streak data for tracking daily engagement.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The unique identifier of the user associated with this streak."
        },
        "currentStreak": {
          "type": "number",
          "description": "The user's current streak in days."
        },
        "longestStreak": {
          "type": "number",
          "description": "The user's longest streak ever achieved in days."
        },
        "lastActiveDate": {
          "type": "string",
          "format": "date-time",
          "description": "The ISO 8601 timestamp of the user's last recorded activity."
        }
      },
      "required": ["userId", "currentStreak", "longestStreak", "lastActiveDate"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/userStreaks/{userStreakId}": {
      "schema": { "$ref": "#/entities/UserStreak" },
      "description": "Stores individual user streak data. The document ID should be the same as the user's UID for easy lookup."
    }
  }
}